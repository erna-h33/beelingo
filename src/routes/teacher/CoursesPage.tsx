import { BookOpen } from "lucide-react"

import { PlaceholderPage } from "@/components/placeholder-page"

export default function CoursesPage() {
  return (
    <PlaceholderPage
      icon={BookOpen}
      title="Courses"
      description="Create courses, add classes, generate class codes and QR codes, and manage your student roster."
      milestone="M3"
    />
  )
}
